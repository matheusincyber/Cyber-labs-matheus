# Contributing / How to use this repo

1. Work in a branch: `git checkout -b feature/your-topic`
2. Add notes & evidence in `notes/` and labs in `3_CAMPAIGN/`
3. Use `scripts/` to assist with reporting (e.g., nmap_xml_to_md.py)
4. Commit and open a Pull Request to `main`.
